import { Container } from "react-bootstrap";

const Title = () => {
    return (
        <Container className="bg-primary text-center my-5 py-4">
            <h1 className="display-1">Formulario</h1>
        </Container>
    );
};

export default Title;